spring:
  application:
    name: store
  datasource:
    url: "${SPRING_DATASOURCE_URL}"

websiteUrl: https://mystore.com

# Para la API de Swagger
springdoc:
  api-docs.enabled: false
  swagger-ui.enabled: false
